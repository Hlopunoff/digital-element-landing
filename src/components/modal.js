export const modal = () => {
  const menuBtn = document.querySelector('.cta__btn');

  // const showModal = () => {

  // };

  menuBtn.addEventListener('click', () => {
    // Executed code here
  });
};
